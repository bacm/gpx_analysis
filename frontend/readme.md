# 🌐 GPX Analysis Frontend

Frontend for the GPX analysis project built with [Next.js](https://nextjs.org/). This project uses the classic `pages/` directory structure (not the `app/` directory).

---

## 🚀 Getting Started
```
npm install
npm run build
npm start
```